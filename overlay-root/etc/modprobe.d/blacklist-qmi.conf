# cdc_wdm/qmi_wwan claim the SIM7600G as a network interface, which prevents
# ModemManager from talking AT commands on ttyUSB2/3 for voice/SMS.
# Per the ClockworkPi wiki recommendation.
blacklist qmi_wwan
blacklist cdc_wdm
